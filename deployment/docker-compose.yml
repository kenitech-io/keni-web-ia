services:
  web:
    image: devops-registry.kenitech.io/keni-web:${KENI_WEB_VERSION:-latest}
    container_name: keni-web-demo
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:3000/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=balancer"
      - "traefik.http.routers.keniweb.entryPoints=web"
      - "traefik.http.routers.keniweb.rule=Host(`demo.kenitech.io`)"
      - "traefik.http.services.keniweb.loadbalancer.server.port=3000"
    networks:
      - balancer
    deploy:
      resources:
        limits:
          memory: 256M

networks:
  balancer:
    external: true
